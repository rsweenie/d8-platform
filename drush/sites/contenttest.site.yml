dev:
  root: /var/www/html/creighton.01dev/docroot
  uri: contenttest.dev-creighton.acsitefactory.com
  host: staging-1749.enterprise-g1.hosting.acquia.com
  options: {  }
  paths: { dump-dir: /mnt/tmp }
  user: creighton.01dev
  ssh: { options: '-p 22' }
test:
  root: /var/www/html/creighton.01test/docroot
  uri: contenttest.test-creighton.acsitefactory.com
  host: staging-1749.enterprise-g1.hosting.acquia.com
  options: {  }
  paths: { dump-dir: /mnt/tmp }
  user: creighton.01test
  ssh: { options: '-p 22' }
live:
  root: /var/www/html/creighton.01live/docroot
  uri: contenttest.creighton.acsitefactory.com
  host: web-1747.enterprise-g1.hosting.acquia.com
  options:
    verbose: true
  paths: { dump-dir: /mnt/tmp }
  user: creighton.01live
  ssh: { options: '-p 22' }
  # command:
  #   core:
  #     rsync:
  #       source: '@contenttest.live:${live.root}/sites/g/files/indaly711/files'
  #       target: '@self:/var/www/creighton/docroot/sites/default/files/'
  #       include-paths:
  #         - ${live.root}/sites/g/files/indaly711/files
  #       exclude-paths:
  #         - ${live.command.core.rsync.include-paths}/acsf_tmp
  #         - ${live.command.core.rsync.include-paths}/bootstrap
  #         - ${live.command.core.rsync.include-paths}/media-icons
  #         - ${live.command.core.rsync.include-paths}/xmlsitemap
  #         - ${live.command.core.rsync.include-paths}/.htaccess

