local:
    root: /var/www/creighton/docroot
    uri: 'http://local.creighton.com'
    host: local.creighton.com
    user: vagrant
    ssh: { options: '-o PasswordAuthentication=no -i $HOME/.vagrant.d/insecure_private_key' }
